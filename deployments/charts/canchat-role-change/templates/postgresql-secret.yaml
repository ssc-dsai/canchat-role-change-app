{{- if kindIs "string" .Values.postgresql.password }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "canchat-role-change.fullname" . }}-postgresql-secret
  labels: {{- include "canchat-role-change.labels" . | nindent 4 }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ .Values.postgresql.password | b64enc | quote }}
{{- end }}